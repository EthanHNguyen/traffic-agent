[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = ["-m", "not integration"]
markers = [
    "integration: tests that require live services, network, credentials, or a local database",
]

[tool.ruff]
line-length = 100
target-version = "py311"
